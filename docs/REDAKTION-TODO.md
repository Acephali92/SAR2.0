# Redaktion-TODO

Sammelstelle für unbelegte Aussagen, offene inhaltliche Lücken und bekannte technische Probleme, die vor einem Live-Launch geklärt werden sollten. Referenziert in `CLAUDE.md`.

## Anforderungs-Traceability (M1–M10, S1–S4)

Abgleich der Muss-/Soll-Anforderungsliste des Referenz-CMS-Lastenhefts mit dem aktuellen Stand (Stand 2026-09-18). Details zu den einzelnen Lücken stehen in den jeweils verlinkten Abschnitten unten.

| # | Anforderung | Status |
|---|---|---|
| M1 | Login E-Mail/Passwort, ohne Git/GitHub | ✅ Erledigt (`cms/src/collections/Users.ts`) |
| M2 | Rollen Autor/Redaktion/Admin | ✅ Erledigt — Schreibrechte korrekt; Leserechte waren zu weit, siehe „access.read" unten (**geschlossen**) |
| M3 | Deutsche UI + Rich-Text-Editor | ✅ Erledigt (`payload.config.ts` i18n, `src/lib/editorConfig.ts`) |
| M4 | Bild/PDF-Upload, Verkleinerung, Pflicht-Alt-Text | ✅ Erledigt — Größenvarianten sind jetzt WebP, siehe „Bildgrößen" unten (**geschlossen**) |
| M5 | Entwurf→Freigabe→Veröffentlichung + zeitgesteuert | ✅ Erledigt (`freigabeStatus`, `cms/src/jobs/schedulePublish.ts`) |
| M6 | Beitrag/Termin mit festen Feldern + Verknüpfung | ⚠️ Teilweise — eingebettete Bilder/PDFs im Fließtext werden jetzt gespiegelt (**geschlossen**); das Titelbild erscheint aber nur als Social-Media-Vorschau (`og:image`), **nicht sichtbar im Artikel** — Platzierung ist eine offene Gestaltungsentscheidung, siehe „Titelbilder aus dem CMS" unten |
| M7 | Statische, cookiefreie Seite, kein React, CSP `script-src 'self'` | ⚠️ Teilweise — versionierte Beispiel-Configs jetzt im Repo, siehe „CSP-Header-Auslieferung" unten (Webserver-/Hosting-Wahl bleibt offen) |
| M8 | EU-Hosting, keine Daten an US-Dienste | ⚠️ Teilweise — Gravatar-Punkt **geschlossen**, SMTP-Anbieter offen (siehe „SMTP-Anbieter" unten) |
| M9 | Änderungen innerhalb weniger Minuten live | ✅ Erledigt — Rebuild-Trigger verpasste Veröffentlichungen und Korrekturen, siehe „Rebuild-Trigger" unten (**geschlossen**) |
| M10 | Tägliche Backups, dokumentierte Wiederherstellung, Export als MD/JSON | ⚠️ Teilweise — Export lief nie und ist jetzt repariert und gegen echte Daten geprüft (**geschlossen**, siehe „Export-Skript" unten); Offsite-Mechanismus vorhanden, konkreter Anbieter bleibt offen (siehe „Offsite-Backup" unten) |
| S1 | Vorschau vor Veröffentlichung | ✅ Erledigt (`cms/src/components/VorschauView.tsx`) |
| S2 | Versionsverlauf mit Wiederherstellung | ✅ Erledigt (`versions.maxPerDoc`, `cms/src/hooks/restrictVersionRestore.ts`) |
| S3 | Redaktions-Übersicht | ✅ Erledigt (`cms/src/components/RedaktionsUebersicht.tsx`) |
| S4 | Pflege durch Docker-Einsteiger ohne Framework-Tiefe | ✅ Erledigt — DB-Migrationen laufen beim Containerstart automatisch; die erste Umsetzung hätte den Container nicht starten lassen, siehe „LAUNCH-BLOCKER" unten (**geschlossen und im Container verifiziert**) |

## Technische Probleme

- **GESCHLOSSEN (2026-09-18): Newsletter-Formular ohne Backend.** `src/pages/index.astro` postete an `action="/api/newsletter"`. Das Projekt ist aber `output: 'static'` (siehe `astro.config.mjs`) — diese Route existiert nicht. Das Formular zeigte nur eine simulierte Erfolgsmeldung (client-seitiges `setTimeout`), sendete aber keine E-Mail-Adresse irgendwohin. *Fix:* wie in `guidelines.md` §14.3 vorgesehen durch einen `mailto:info@stoppramstein.de`-Link ersetzt (gleicher CTA-Button-Pfad wie die übrigen drei Hero-Slides); das separate Formular-Markup und der zugehörige Fake-Submit-Handler in `public/scripts/homepage.js` sind entfallen.

## Screenshot

- `docs/screenshot.png` wurde aus `screenshots/Screenshot 2026-09-16 182321.png` übernommen (manueller Screenshot der Startseite, da automatisierte Erstellung per Browser-Tool in dieser Session nicht möglich war). Bei Layout-Änderungen an der Startseite aktualisieren.

## Unbelegte Aussagen

*(Noch keine gemeldet. Beim Redigieren von Inhalten in `src/content/warum-ramstein/` bzw. Beiträgen im CMS hier eintragen, wenn eine Aussage keine Quelle hat.)*

## Payload-CMS: offene Punkte

- **TEILWEISE GESCHLOSSEN (2026-09-18): Offsite-Backup-Mechanismus.** Das nächtliche Backup (`cms/backup/backup.sh`) lag nur lokal auf dem Server (14 Tage Aufbewahrung). *Fix:* `backup.sh` kopiert die frischen Archive jetzt zusätzlich per `rclone` an ein konfigurierbares Ziel, wenn `OFFSITE_RCLONE_REMOTE` (siehe `cms/.env.example`) gesetzt ist und `secrets/rclone.conf` existiert — ohne Konfiguration bleibt das Verhalten unverändert (nur lokal). `cms/backup/Dockerfile` installiert `rclone` bereits mit. **Weiterhin offen:** welcher zweite, räumlich getrennte EU-Anbieter tatsächlich genutzt wird, ist eine Betreiberentscheidung — Einrichtung ist in `docs/DEPLOYMENT.md`, Abschnitt "Backups & Wiederherstellung", dokumentiert.
- **SMTP-Anbieter:** Für Payloads Passwort-Reset-E-Mails und die Rebuild-Fehlerbenachrichtigung (`cms/.env.example`, `SMTP_*`) fehlt noch ein konkreter, DSGVO-konformer EU-E-Mail-Versanddienst.
- **Zurückgestellte Komfort-Funktionen — umgesetzt.** Redaktions-Übersicht, Versions-Wiederherstellungsrechte und Vorschau vor Veröffentlichung sind gebaut (siehe `docs/INHALTE-PFLEGEN.md` Teil B und `docs/ARCHITEKTUR.md`). Verbliebene Einschränkungen:
  - **Öffentliche Preview-URL bewusst nicht gebaut.** Die Vorschau lebt ausschließlich in der angemeldeten Admin-Oberfläche. Ein Link zum Teilen mit Außenstehenden würde entweder SSR auf der öffentlichen Seite oder ein Token-Cookie erfordern — beides widerspricht der statischen, cookiefreien Architektur.
  - **Vorschau ist keine pixelgenaue Kopie.** Header/Footer der öffentlichen Seite fehlen bewusst; Schriftfamilien werden nur benannt (IBM Plex ist in `cms/` nicht selbst gehostet, es greift der Fallback). Für die Beurteilung von Text, Bild, Quellen und Verknüpfungen reicht das; für ein Layout-Urteil bis auf den Pixel nicht.
  - **Media-Restore-Rechte offen, aber gegenstandslos:** `Media` hat aktuell keine `versions` aktiviert, es gibt dort also nichts wiederherzustellen. Wird das später eingeschaltet, muss `canReadVersions` (bzw. eine Admin-Variante davon) dort ebenfalls an `access.readVersions` gehängt werden.

- **GESCHLOSSEN (2026-09-18): `access.read` auf `beitraege`/`termine` war für alle per Session Angemeldeten offen.** Code und Dokumentation gingen auseinander: `docs/INHALTE-PFLEGEN.md` und `cms/README.md` sagen „Autor sieht nur eigene Dokumente", `access.read` gab aber jedem mit Session eingeloggten Nutzer Lesezugriff auf alles (die Rollenlogik griff bisher nur bei `update`/`delete`). *Fix:* `cms/src/access/readPublishedOrSession.ts` prüft für Session-Logins jetzt die Rolle — Redaktion/Admin sehen weiterhin alles, Autor nur noch Veröffentlichtes oder eigene Dokumente (`{ or: [nurVeroeffentlicht, { createdBy: { equals: req.user.id } }] }`, analog zu `ownDraftOrRedaktion.ts`). Damit stimmen Code und Doku jetzt überein; Redaktions-Übersicht und Vorschau setzten die Einschränkung ohnehin schon selbst durch und sind unverändert. (API-Key-Zugriffe sind davon nicht betroffen, siehe nächster Punkt.)

- **GESCHLOSSEN (2026-09-18): Entwürfe und Mediathek waren über die API anonym lesbar.** Beim Sicherheitsaudit live nachgewiesen, seit Einführung der CMS-Integration vorhanden. Ob eine produktiv erreichbare Instanz betroffen war, ist hier nicht bekannt — falls ja, müssen Entwurfsstände aus diesem Zeitraum als potenziell öffentlich gelten.
  - *Was lesbar war:* Ohne Login lieferte `GET /api/beitraege/<id>?draft=true` (ebenso GraphQL mit `draft: true`) bei jedem veröffentlichten Beitrag den **neuesten, unveröffentlichten Entwurf** — Titel und Volltext von Korrekturen oder Überarbeitungen, bevor die Redaktion sie freigegeben hatte. `termine` hatte dieselbe Regel. Zusätzlich war `GET /api/media` samt Dateien öffentlich — also auch Bilder, die nur in unveröffentlichten Entwürfen steckten. Reine, nie veröffentlichte Entwürfe waren **nicht** lesbar (404).
  - *Ursache:* `access.read` für Unangemeldete prüfte nur `freigabeStatus`. Dieser bleibt `veroeffentlicht`, wenn nach der Veröffentlichung ein Entwurf gespeichert wird; Payload liefert mit `draft=true` dann die neueste Version, solange `_status` nicht mitgeprüft wird. `Media` hatte `read: () => true`.
  - *Fix:* `cms/src/access/readPublishedOrSession.ts` verlangt für anonyme **und** API-Key-Zugriffe `freigabeStatus = veroeffentlicht` UND `_status = published`. Versionen sind für API-Keys gesperrt. `Media` ist nur noch für Angemeldete lesbar; `payload-loader.ts` lädt Bilder deshalb mit dem API-Key. Der Job für zeitgesteuerte Veröffentlichung setzt jetzt auch `_status: published` — sonst wäre ein geplanter Beitrag trotz Freigabe unsichtbar geblieben.
  - *Folge für die Redaktion:* Ein Beitrag erscheint nur noch, wenn der Status „Veröffentlicht" ist **und** in Payload „Änderungen veröffentlichen" geklickt wurde. Bisher reichte faktisch der Status allein. Siehe `docs/INHALTE-PFLEGEN.md`, „Freigabe und Veröffentlichung".
  - *Bewusst verbleibend:* Der Build-Nutzer (API-Key) kann die komplette Media-Liste (Metadaten, auch reine Entwurfsbilder) und die Nutzerliste lesen. Das braucht der Loader nicht vollständig — er lädt nur Bilder veröffentlichter Dokumente herunter und braucht aus `users` nur den Autorennamen —, eine feinere Regel wäre aber deutlich aufwendiger. Der Key ist ein Server-Geheimnis, kein öffentlicher Zugang.

- **TEILWEISE GESCHLOSSEN (2026-09-18): Titelbilder aus dem CMS wurden auf der Website nicht angezeigt.** `payload-loader.ts` lädt `image` herunter und legt es als `data.image` ab, aber weder `src/pages/analysen/[...slug].astro` noch `ArticleLayout.astro` renderten das Feld — beim Live-Build-Test (2026-09-18) landete das Bild in `dist/media/cms/`, wurde aber von keiner Seite referenziert. *Fix:* `ArticleLayout.astro` bekommt (analog zu `EventLayout.astro`) eine `image`-Prop und nutzt sie als `ogImage`; `analysen/[...slug].astro` reicht `entry.data.image` durch. Beiträge haben damit wie Termine ein Social-Preview-Bild.
  - **Weiterhin offen: Das Titelbild ist im Artikel nicht sichtbar.** Es erscheint nur in `og:image`/`twitter:image` (im Build 2026-09-18 geprüft). Wo es im Artikel sitzen soll, ist eine Gestaltungsentscheidung — Vorschlag: unter dem Anrisstext, wie in der CMS-Vorschau.
  - **GESCHLOSSEN (2026-09-18): Eingebettete Bilder und PDFs im Fließtext.** Der HTML-Konverter setzte die absolute CMS-URL ein (`<img src>`, `<source srcset>`, bei PDFs `<a href>` auf `…/api/media/file/…`), `payload-loader.ts` übernahm `renderedHtml` unverändert. Die Seite hätte das CMS zur Laufzeit gebraucht; im Browser scheiterte es an der CSP (`img-src 'self'`) und seit dem Leak-Fix zusätzlich am fehlenden Login. Veröffentlichte Inhalte mit eingebetteten Bildern gab es zu diesem Zeitpunkt nicht (Fixtures, `dist/` und Datenbank geprüft). *Fix:* `payload-loader.ts` (`spiegleEingebetteteMedien`) lädt jede eingebettete CMS-Datei mit dem API-Key über `PAYLOAD_URL` herunter und schreibt `src`/`srcset`/`href` auf `/media/cms/` um; eingebettete Bilder fremder Herkunft lassen den Build scheitern. `scripts/check-csp.mjs` schlägt jetzt zusätzlich bei Bildern fremder Herkunft und jedem Verweis auf `/api/media/` an — im Rebuild-Webhook bleibt dann die alte Seite online. *Verifiziert:* Beitrag mit eingebettetem Bild und PDF gebaut. Mit dem alten Loader fand der Check alle fünf CMS-Verweise (Exit 1), mit dem neuen keinen. Bei **gestopptem CMS** lieferte die statische Seite Artikel, alle Bildvarianten und das PDF aus (je HTTP 200).
  - **GESCHLOSSEN (2026-09-18): Eingebettete Bilder wurden auf mittleren Bildschirmbreiten beschnitten.** Payloads Standard-Upload-Konverter bot jede Bildgröße als `<source>` an, auch den 1200×630-Zuschnitt `og` (`fit: cover`, für Social Media). Beobachtet: Bei 1000 px Breite zeigte der Browser diesen Zuschnitt eines 1600×490-Banners, rund 40 % der Bildbreite fehlten. *Fix:* eigener Upload-Konverter in `cms/src/lib/lexicalToHtml.ts` — ein `<img>` mit `srcset` nur aus seitenverhältnis-treuen Größen (`thumbnail`, `card`, Original), dazu `width`/`height` und `loading="lazy"`. *Verifiziert* bei 1000 px und auf dem Handy: Seitenverhältnis wie im Original, nichts beschnitten.

- **GESCHLOSSEN (2026-09-18): LAUNCH-BLOCKER — keine Datenbank-Migrationen.** Payloads Postgres-Adapter legt Tabellen nur außerhalb von `NODE_ENV=production` automatisch an („push", siehe `@payloadcms/db-postgres/dist/connect.js`). Das Dockerfile setzt `NODE_ENV=production`, und im Repo gab es kein `cms/src/migrations/`. Live am laufenden lokalen Dev-Container nachgewiesen: `payload_migrations` enthielt nur den internen Marker `{ name: 'dev', batch: -1 }` — genau das Zeichen für ein per Push statt per Migration entstandenes Schema.
  - *Fix:* `payload migrate:create initial_schema` gegen die laufende Dev-Datenbank ausgeführt → `cms/src/migrations/20260918_143807_initial_schema.ts` (+ `index.ts`) erzeugt, das komplette Schema abbildend.
  - *Verifiziert (kein Trockenschwimmen):* eine leere Scratch-Datenbank (`payload_migration_test`) im selben Postgres-Container angelegt, `payload migrate` dagegen laufen lassen — alle 22 Tabellen entstanden fehlerfrei und stimmen exakt mit denen der bestehenden Dev-Datenbank überein. Scratch-Datenbank danach wieder gelöscht; die Dev-Datenbank selbst wurde nicht verändert.
  - ~~*Fix (Rollout):* `cms/Dockerfile` führt vor dem Start `npx payload migrate` aus.~~ **Das funktionierte nicht (2026-09-18 im Container nachgewiesen):** Das Runner-Image enthält weder `src/payload.config.ts` noch `src/migrations/` noch `tsconfig.json`. `npx payload migrate` brach mit `TypeError: Cannot read properties of null (reading 'config')` ab, der Container endete mit Exit 1, die Datenbank blieb leer — der erste Produktions-Deploy wäre ein Totalausfall des CMS gewesen.
  - *Korrigierter Rollout:* Die Migrationen sind jetzt über `prodMigrations` im Postgres-Adapter (`cms/src/payload.config.ts`) mitgebündelt und laufen beim Serverstart; das Dockerfile setzt dafür `PAYLOAD_AUTO_MIGRATE=true` und startet wieder schlicht mit `npm run start`. *Im Container verifiziert:* gegen eine leere Datenbank entstanden alle 22 Tabellen, die Migration steht als Batch 1 in `payload_migrations`, API und erster Nutzer funktionieren; ein Neustart migriert nicht erneut.
  - **Einschränkung, bewusst so gebaut:** Die automatische Migration läuft nur im Docker-Image. Findet sie eine per Dev-„Push" veränderte Datenbank (Markierung `dev`, `batch -1`), stellt Payload eine interaktive Rückfrage — ohne Terminal **hängt der Server daran** (gegen eine Kopie der Dev-Datenbank beobachtet: Container läuft, antwortet nie). Ein lokaler `npm run start` gegen die Dev-Datenbank wäre sonst unbenutzbar. Produktiv deshalb nur eine frische oder ausschließlich per Migration gepflegte Datenbank verwenden (siehe `cms/README.md`, „Migrationen").
  - *Für lokale Entwicklung ohne Auswirkung:* Die laufende Dev-Datenbank bleibt im Push-Modus (kein `NODE_ENV=production` lokal) und wurde nicht angefasst.

- **GESCHLOSSEN (2026-09-18): Gravatar in der Admin-Oberfläche.** Payload lud das Konto-Symbol standardmäßig von `gravatar.com` (`admin.avatar: 'gravatar'`, Payloads Default) — bei jedem Seitenaufruf ging ein Hash der E-Mail-Adresse der angemeldeten Person an einen US-Dienst. Betraf nur `redaktion.stoppramstein.de`, nicht die öffentliche Seite. *Fix:* `admin: { avatar: 'default' }` in `cms/src/payload.config.ts`.

- **Restliche Advisories im CMS (7× mittel/niedrig):** stecken in Payloads eigenen Abhängigkeiten — `drizzle-kit`/`esbuild` (nur Entwicklungswerkzeug, vom Build ausgeschlossen) und `dompurify` im Monaco-Code-Editor der Admin-UI. Innerhalb von Payload 3.89 nicht behebbar; bei künftigen Payload-Updates erneut `npm audit` prüfen.

- **GESCHLOSSEN (2026-09-18): Root-Projekt hatte noch sharp 0.33.5** mit denselben libvips-Advisories wie vorher das CMS. Dort nur zur Build-Zeit im Einsatz (`scripts/generate-assets.mjs`, Astro-Bildverarbeitung), verarbeitete also keine fremden Uploads — geringeres Risiko, aber zwecks Parität mit `cms/` (sharp 0.35.4) auf dieselbe Version angehoben. `npm run build` lief danach unverändert durch.

- **GESCHLOSSEN (2026-09-18): Bildgrößen wurden als JPEG statt WebP erzeugt.** `formatOptions` in `cms/src/collections/Media.ts` galt nur fürs Original; `thumbnail`/`card`/`og` blieben JPEG (Payloads `ImageSize`-Typ verlangt `formatOptions` explizit pro Größe — ein collection-weiter Default reicht nicht). *Fix:* jeder der drei `imageSizes`-Einträge hat jetzt ein eigenes `formatOptions: { format: 'webp', options: { quality: 80 } }`. *Verifiziert* per Payload Local API gegen die laufende Dev-Datenbank: ein Testbild erzeugte `thumbnail` tatsächlich als `image/webp` (`test-webp-verify-400x400.webp`, danach wieder gelöscht). `card`/`og` ließen sich mit demselben Testbild nicht zusätzlich verifizieren, da es dafür zu klein war (Payload überspringt Größen, die das Quellbild vergrößern müssten) — nutzen aber exakt dieselbe `formatOptions`-Struktur wie das erfolgreich getestete `thumbnail`.

- **GESCHLOSSEN (2026-09-18): Export-Skript (`cms/scripts/export-content.mjs`) filterte auf falsches Feld.** `payload.find` bekam `where: { status: { equals: 'veroeffentlicht' } }`, das Feld heißt aber `freigabeStatus` (siehe Kommentar in `cms/src/fields/statusField.ts` zur Namenskollision mit Payloads generiertem Postgres-Enum `_status`). Payload ignoriert unbekannte `where`-Schlüssel, d. h. der Filter griff nicht und `npm run export` exportierte vermutlich auch unveröffentlichte Entwürfe. *Fix:* Feldname auf `freigabeStatus` korrigiert.
  - **Der echte Export-Lauf (2026-09-18) zeigte drei weitere Fehler, alle behoben:**
    - *`npm run export` lief nie:* Reines `node` kann `src/payload.config.ts` mit ihren endungslosen Imports nicht laden (`ERR_MODULE_NOT_FOUND`, auch unter Node 24). Jetzt `payload run scripts/export-content.mjs`; das Skript braucht dafür ein Top-Level-`await`, weil `payload run` direkt nach dem Import `process.exit(0)` aufruft.
    - *Öffentlich unsichtbare Beiträge wurden exportiert:* Der Filter prüfte nur `freigabeStatus`. Jetzt dieselbe Definition wie der öffentliche Lesezugriff (`freigabeStatus = veroeffentlicht` UND `_status = published`).
    - *`all.json` enthielt Nutzerdaten der Redaktion:* Mit `depth: 1` wurde `createdBy` vollständig aufgelöst, samt E-Mail, Rolle, Session-IDs und dem Feld `apiKey`. Ein aktivierter API-Key wäre im Klartext im Export gelandet. Jetzt `populate: { users: { name: true } }`.
  - *Verifiziert* mit einem veröffentlichten Beitrag, einem veröffentlichten Beitrag mit neuerem Entwurf, einem nur per `freigabeStatus` „veröffentlichten" und einem reinen Entwurf: exportiert wurden genau die öffentlich sichtbaren (beim Beitrag mit Entwurf die veröffentlichte Fassung), keine personenbezogenen Daten.

- **GESCHLOSSEN (2026-09-18): Rebuild-Trigger verpasste Veröffentlichungen und Korrekturen.** `triggerRebuild` feuerte nur beim Wechsel von `freigabeStatus` auf oder von „veröffentlicht". Seit dem Leak-Fix ist ein Beitrag aber erst öffentlich, wenn zusätzlich „Änderungen veröffentlichen" geklickt wurde. Gegen den alten Trigger live nachgewiesen:
  - Bei „Status umstellen, als Entwurf speichern, dann veröffentlichen" wurde beim Speichern unnötig gebaut und bei der eigentlichen Veröffentlichung **gar nicht** — der Beitrag erschien nicht von selbst.
  - **Veröffentlichte Korrekturen** an laufenden Beiträgen lösten keinen Rebuild aus.

  *Fix:* `cms/src/hooks/triggerRebuild.ts` vergleicht die Hauptzeile des Dokuments in der Datenbank vor und nach der Änderung. Payload schreibt sie nur bei Nicht-Entwurfs-Speicherungen, Entwurf und Autosave lösen also nie etwas aus. Gebaut wird, wenn sich die Hauptzeile geändert hat und das Dokument vorher oder nachher öffentlich sichtbar war. *Verifiziert* mit einem lokalen Webhook-Empfänger in elf Fällen: Entwurf anlegen, Autosave, Status nur als Entwurf, Veröffentlichen, Korrektur als Entwurf, Korrektur veröffentlicht, Zurückziehen, nie öffentliches Dokument, Version wiederherstellen, direkt veröffentlicht anlegen, zeitgesteuert per Job. Der neue Trigger bestand alle elf, der alte scheiterte an drei.

- **TEILWEISE GESCHLOSSEN (2026-09-18): CSP-Header-Auslieferung (M7) war nicht Teil des Repos.** `scripts/check-csp.mjs` prüft nur, dass der Build-Output keine Inline-Scripts enthält — das ist eine Vorbedingung, aber keine Garantie, dass der produktive Webserver tatsächlich `Content-Security-Policy: script-src 'self'` sendet. Es lag weder eine Caddyfile noch eine nginx-Config im Repository. *Fix:* `deploy/Caddyfile.example` und `deploy/nginx.conf.example` versioniert ergänzt (identischer CSP-Header-String wie in `scripts/write-preview-headers.mjs`); `docs/DEPLOYMENT.md` verweist jetzt darauf. Die Caddy-Variante wurde per `caddy validate` (Docker-Image `caddy:2-alpine`) auf syntaktische Korrektheit geprüft. **Weiterhin offen:** welcher der beiden Webserver und welcher Hosting-Anbieter tatsächlich produktiv läuft, ist eine Betreiberentscheidung — erst danach lässt sich die gewählte Vorlage mit echter Domain/TLS ausrollen.

- **Kosmetisch:** Payloads deutsche Übersetzung zeigt in der Versionsliste „Sortieren nach {{label}}" (nicht aufgelöster Platzhalter in Payload selbst).

- **GESCHLOSSEN (2026-09-18): Next.js 15.4.11 mit kritischen Advisories im CMS.** Upgrade auf Next.js 16.3.5 (Turbopack) und sharp 0.35.4; `npm audit` im CMS: kritisch 1 → 0, hoch 2 → 0. Payload 3.89 unterstützt Next.js nur `<15.5` oder `>=16.2.6`, der 15.5-Patchzweig schied daher aus. Getestet: Produktions-Build lokal und im Docker-Image (Linux/Alpine), Start des Images gegen die Datenbank, Bild-Upload mit Größenvarianten über sharp im Container, Admin-Oberfläche im Browser (Dashboard, Bearbeiten mit Autosave, Vorschau, Versionen, Versionsvergleich; keine Konsolenfehler), Rollen- und Zugriffsregeln per REST, zeitgesteuerte Veröffentlichung über den Cron des Produktionsservers, Astro-Build gegen das CMS. Dabei mitbehoben: (a) das Docker-Image war nie baubar (`COPY /app/public` ohne `public/`-Verzeichnis), (b) die zwei Typfehler in `scripts/seed-import.ts` ließen `next build` mit lokal generierten Typen scheitern — auf frischem Checkout traten sie nicht auf, der Docker-Build war davon also nicht betroffen, (c) `seed-import.ts` setzte kein `_status: 'published'` und hätte seit dem Leak-Fix unsichtbare Beiträge importiert, (d) Turbopack wählte wegen des zweiten Lockfiles das Repo-Wurzelverzeichnis (samt dessen `node_modules`) als Wurzel, jetzt fest auf `cms/` gesetzt.
